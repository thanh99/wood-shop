const model = {}

model.saveListProduct = (listProduct) => {
    model.listProduct = listProduct
}

model.saveNumberOfPage = (numberOfPage) => {
    model.numberOfPage = numberOfPage
}

model.saveCurrentPage = (currentPage) => {
    model.currentPage = currentPage
}